# Claude Code local settings (personal preferences, not for git)
.claude/settings.local.json

# Common
.DS_Store
Thumbs.db
node_modules/
.env
.env.local

# Hebrew-text review exports (internal working docs, not for the public repo)
HEBREW_TEXTS*.md
HEBREW_TEXTS*.csv
_apply_hebrew.js

# Marketing scout - lives in its own PRIVATE repo, never in the public app repo
marketing-scout/

# Landing-page makeover PREVIEW - local only, not public until approved
landing-preview*.html

# App redesign mockup GALLERY - local only, not public until approved
design-gallery*.html

# Brand avatar generator (local)
brand-avatar.html
friendlybet-avatar*.png

# Brand banner generator (local)
brand-banner.html
friendlybet-banner*.png

# brand source images (kept in private repo, not the public site)
friendlybet-avatar.png
friendlybet-banner.png

# diagnostic screenshots (local)
_diag-*.png

# local marketing media samples (not part of the site)
samples/

# local video previews (not for publishing/commit)
DEMO-dynamic-video.mp4
DEMO-dynamic-thumb.png
DEMO-*.mp4
DEMO-*.png

# YouTube batch local workspace (large MP4s + intermediates, not for the public repo)
youtube-videos/

# Downloaded Wikipedia player portraits (local only — fetched via scripts/download-player-photos*.js)
player-photos/
player-photos-multi/
player-photos-multi-run.log
.seo-gen/

# stray local promo render (not for the public repo)
friendlybet_promo.mp4
.playwright-mcp/

# local-only prototypes (owner preview, never deployed / never shown to users)
_prototypes/

# local-only marketing / SEO playbooks (owner notes, never public)
_private/

# internal AI/dev context (owner-only, not for the public repo)
CLAUDE.md
.claude/

# stray local artifacts (unreferenced, not for the public repo)
/wizard-step2-scoring.png
/matches_sql.sql
/pin/

# scratch OG render checks
_og_*.png
_story_review_created.json

# scratch / preview artifacts (champion card work)
_live_test/
_qa-artifacts/
/card-*.png
/card-b64*.txt
/champ-card*.png
/canvas-render*.png
/esp-*.png
